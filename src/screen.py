class Screen:
    def __init__(self) -> None:
        self.frame = None

#Class to handle the drawing of all the various visual elements
#   drawMap(map, player_units, enemy_units) {
#           self.size = map.getSize()
#           Create the Window
#           pull the tilemap for the map
#           place all the map triggers
#           place all the enemy_units on the grid
#           place all the player_units on the grid
#           center the cursor on player_units[0] 
#           
#        }
